# 外部账套页面自动化边界

公开源树不保存任何真实外部账套的页面地址、选择器、坐标、账套名、测试结果
或导入记录。这些资料会随页面版本和授权环境变化，公开副本既不构成可执行
配方，也不应被当作外部写入授权。

真实页面自动化属于 W10 范围。只有在用户当回合明确授权、测试环境身份已
确认且屏幕未被占用时，操作员才能在私有工作区重新采集当前事实。采集结果
不得回写到公开仓库；任何真实导入、读回或 reconcile-only 操作仍需独立的
当回合授权。
